import { withKnobs, text, select } from "@storybook/addon-knobs";
import { Meta, Story,Props, Preview } from '@storybook/addon-docs/blocks';

import Button from './button.jsx'

<Meta title="Atoms|Buttons" component={Button}/>

## Buttons
Button is a default component to display action in a page.

<Preview withToolbar>
    <Story name="Button">
        <Button>{text('text', 'Button')}</Button>
    </Story>
</Preview>

<Props of={Button} />

## USAGE

Import your `button component` in your file

```jsx
import { Button } from "../your_path/components";

```

```jsx
<Button>Button</Button>
```

### Playground

Experiment with this story with Knobs addon in Canvas mode.

<Story name="knobs" parameters={{ decorators: [withKnobs] }}>
  <Button variant={select('variant', ["primary", "secondary"])}>{text('text','Button')}</Button>
</Story>
