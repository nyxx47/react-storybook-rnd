import { withKnobs, text, select } from "@storybook/addon-knobs";
import { Meta, Story,Props, Preview } from '@storybook/addon-docs/blocks';

import Input from './input.jsx'

<Meta title="Atoms|Input" component={Input}/>

## Input
Input allow users to enter text into a UI.

<Preview withToolbar>
    <Story name="Input">
        <Input/>
    </Story>
</Preview>

<Props of={Input} />

## USAGE

Import your `Input component` in your file

```jsx
import { Input } from "../your_path/components";

```

```jsx
   <Input />
```
